use std::io;


fn set_up_keys() {

    loop {
        let mut input = String::new();
        io::stdin().read_line(&mut input).expect("Failed to read line");

        //let split = input.split(" ").collect();






    }



}




pub fn setup() {
    

}

pub fn use_defaults() {

    unsafe {

        let mut vec = &mut crate::settings::KEYS;
      

     //   for default_key in crate::settings::DEFAULT_KEYS {
      //      vec.push(default_key)
      //  }
    }

}